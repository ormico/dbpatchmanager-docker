# Project Rules

## Pull Requests
- Every PR must be linked to at least one GitHub issue (use `Closes #N` in PR body).
- Keep PRs scoped to one issue each. Only combine issues if they are genuinely inseparable.
- Do not include Claude attribution in commits, PR descriptions, or code comments.

## Git
- Never push to remote without explicit user instruction.
