name: PR Validation

on:
  pull_request:
    branches: [main]

jobs:
  build-and-test:
    uses: ./.github/workflows/docker-build-push.yml
    with:
      tags: dbpatchmanager-ci-v2:pr-${{ github.sha }}
      push: false
